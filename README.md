
create .env and copy the contents of .env.sample to it. Then fill in the values for the environment variables.

```bash
cp .env.sample .env
``` 

Then install the dependencies and start the server.

```bash
npm install
npm run dev
```

The server will a vite development server on port 5173. You can access the application at http://localhost:5173.